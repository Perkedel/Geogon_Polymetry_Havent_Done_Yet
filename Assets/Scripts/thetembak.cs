using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class thetembak : MonoBehaviour
{
    public GameObject peluru;
    // Start is called before the first frame update
    void Start()
    {
        Instantiate(peluru);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
